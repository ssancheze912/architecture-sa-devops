apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: reconcile-company-base-withholdings
  namespace: liquid-tax
spec:
  type: bindings.cron
  version: v1
  metadata:
    - name: schedule
      value: "@every 1h"
