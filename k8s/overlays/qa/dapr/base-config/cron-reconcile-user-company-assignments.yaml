apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: reconcile-user-company-assignments
  namespace: base-config
spec:
  type: bindings.cron
  version: v1
  metadata:
    # Triggers POST /jobs/reconcile-user-company-assignments on base-config every hour.
    # Dapr binding name must match the registered route path (without leading slash).
    - name: schedule
      value: "@every 1h"
