apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: accounting-api-hc
  namespace: accounting
spec:
  default:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 3
    config:
      type: TCP
      tcpHealthCheck:
        port: 8080
  targetRef:
    group: ""
    kind: Service
    name: accounting-api
