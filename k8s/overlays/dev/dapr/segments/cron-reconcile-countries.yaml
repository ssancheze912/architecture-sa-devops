apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: reconcile-countries
  namespace: segments
spec:
  type: bindings.cron
  version: v1
  metadata:
    # Triggers POST /reconcile-countries on segments-api every hour.
    # Dapr binding name must match the registered route path (without leading slash).
    - name: schedule
      value: "@every 1h"
